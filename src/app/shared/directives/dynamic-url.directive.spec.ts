import { DynamicUrlDirective } from './dynamic-url.directive';

describe('DynamicUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicUrlDirective();
    expect(directive).toBeTruthy();
  });
});
